{
  "name": "makemoneyordie",
  "private": true,
  "scripts": {
    "docker:rebuild": "DOCKER_BUILDKIT=0 docker build -t makemoneyordie-backend-dev --target dev ./backend && DOCKER_BUILDKIT=0 docker build -t makemoneyordie-frontend-dev --target dev ./frontend && docker compose up -d --no-build",
    "generate-secrets": "npm --prefix backend run generate-secrets",
    "setup:prod-env": "node scripts/setup-prod-env.mjs"
  }
}
